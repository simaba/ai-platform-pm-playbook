# Evals as Product Requirements

A feature is not fully specified until:
- the user promise is clear
- the failure modes are named
- the evaluation criteria are measurable
- the release gate is explicit
